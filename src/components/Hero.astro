---
interface Props {
  title: string;
  tagline?: string;
}

const { tagline, title } = Astro.props;
---

<div>
  <h1 class="title">{title}</h1>
  {tagline && <p class="tagline">{tagline}</p>}
  <slot />
</div>
